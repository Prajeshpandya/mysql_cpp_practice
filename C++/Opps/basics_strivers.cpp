// #include <iostream>
// #include <math.h>
// #include <string>
#include <bits/stdc++.h> //it includes all the library;

using namespace std;
int main() {
  // std::cout << "Hello World!" << endl << "hey bro" << endl ;

  // int x;
  // cin >> x;
  // cout << "the value of X : " << x;

  // string str1;
  // string str2;     //it take only one string means before space onlyone part..
  // cin >> str1 >> str2;
  // cout << str1 << " " << str2;

  // so if we want to do more than one string then we use 
  // string str;
  // getline(cin,str);

  char ch = 'a';
  cout << ch;
  
  return 0;
}